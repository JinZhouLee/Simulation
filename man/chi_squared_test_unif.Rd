% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chi_squared_test_unif.R
\name{chi_squared_test_unif}
\alias{chi_squared_test_unif}
\title{Chi-squared goodness-of-fit test for uniform distribution (0, 1)}
\usage{
chi_squared_test_unif(x, k = 100, alpha = 0.95)
}
\arguments{
\item{x}{numerical vector: the data}

\item{k}{positive integer: partition number, recommend n / k > 5, where n is the length of x}

\item{alpha}{numerical value in (0, 1): hypothesis level}
}
\value{
a list, level is the hypothesis level, test_statistic is the Chi-Squared test statistic, chi_squared is the test quantity, accept and reject is the result of the test.
}
\description{
Let \eqn{n} be the size of data \eqn{x},
the chi-squared goodness-of-fit test will partition \eqn{[0, 1]} interval into k equal length subinterval,
and calculate the number of points in the \eqn{j}-th interval \eqn{f_j}.
Then the test statistic is
\deqn{\chi^2 = \frac{k}{n} \sum_{j = 1}^{k} \left( f_j - \frac{n}{k} \right)^2}
We will reject the hypothesis (\eqn{x \sim_{\text{i.i.d.}} \text{Unif} (0, 1)}) at level \eqn{\alpha} if
\deqn{\chi^2 > \chi_{k - 1}^2 (1 - \alpha)}
where \eqn{\chi_{k - 1}^2 (1 - \alpha)} is the \eqn{1 - \alpha} quantile of chi-squared distribution with \eqn{k - 1} degree of freedom
}
\examples{
x = unif_gen(4000)
chi_squared_test_unif(x)
}
